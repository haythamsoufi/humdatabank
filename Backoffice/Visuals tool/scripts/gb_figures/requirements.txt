# Production uses Playwright HTML/SVG (GB_FIGURES_RENDERER=html).
# Matplotlib is a legacy fallback for debugging only.
pandas>=2.0
openpyxl>=3.1
matplotlib>=3.8
numpy>=1.26
quarto>=0.2.0
playwright>=1.40
python-docx>=1.1.0
