# Admin “templates” UI (not Jinja base templates)

This folder holds **survey / form template management** screens (list, create, Kobo import, special templates).

Application chrome and inheritance live in [`core/layout.html`](../../core/layout.html). Reusable Jinja macros and components are under [`components/`](../../components/) and [`macros/`](../../macros/).
