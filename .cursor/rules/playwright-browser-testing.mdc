---
description: Playwright browser testing conventions - quick test logins and hard refresh after code changes
alwaysApply: true
---

# Playwright Browser Testing

## Logging In (Local Dev)

The Backoffice runs at `http://localhost:5000`. In development config, the login page (`/login`) shows a yellow **"Quick login for testers"** box with three one-click buttons (no credentials needed — they auto-submit the login form with seeded test accounts):

| Button id | Logs in as |
|---|---|
| `#quickSystemManagerLogin` | System Manager (`test_sys@<org-domain>`) |
| `#quickAdminLogin` | Admin (`test_admin@<org-domain>`) |
| `#quickFocalLogin` | Focal Point (`test_focal@<org-domain>`) |

Use these buttons whenever a test session is needed — never ask the user for credentials. To switch roles, navigate to `/logout` first, then click a different quick-login button.

## Hard Refresh After Code Changes

After making code changes to static files (JS, CSS, HTML templates), always perform a **hard refresh** before testing in the browser via Playwright MCP.

## Hard Refresh

After navigating to the page, force-reload to bypass the browser cache:

```js
// Hard refresh via keyboard shortcut
await page.keyboard.down('Control');
await page.keyboard.press('F5');
await page.keyboard.up('Control');

// Or via evaluate (most reliable)
await page.evaluate(() => location.reload(true));

// Or navigate with cache bypass
await page.goto(url, { waitUntil: 'networkidle' });
await page.evaluate(() => location.reload(true));
```

Or using the MCP `browser_evaluate` tool:
```json
{ "function": "() => { location.reload(true); return 'hard refreshing'; }" }
```

Then wait for the page to be ready before interacting.

## Why

The browser caches JS/CSS files. Without a hard refresh, Playwright will test against the **old cached version** of modified files, giving misleading results.
