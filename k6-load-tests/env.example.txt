# k6 load-testing environment variables (variable names only — DO NOT commit real values).
#
# Copy this file as a reference; pass values via -e KEY=VALUE on the k6 command line
# or via your shell environment. Never commit a populated .env to the repo.

# --- Phase 1 ---------------------------------------------------------------

# Backoffice origin (Flask app). Backoffice only — NOT the public Website URL.
# Examples:
#   BASE_URL=http://127.0.0.1:5000
#   BASE_URL=https://your-staging-host
BASE_URL=

# Bearer API key for /api/v1 GET scenarios. Create a dedicated, read-only,
# rate-limit-friendly key in staging. Never use a production key.
K6_BACKOFFICE_API_KEY=

# Threshold profile: "local" (relaxed) or "staging" (strict). When unset, the
# profile is auto-detected from BASE_URL: loopback host -> "local", otherwise
# "staging". Set this explicitly to override the auto-detection (e.g. when
# tunneling staging through localhost).
K6_PROFILE=

# --- Phase 1.5 (opt-in scenarios) ------------------------------------------

# Comma-separated submitted-document IDs (e.g. "12,34,56") used by
# k6-load-tests/scenarios/document-download.js to fetch /documents/thumbnail/<id>.
K6_DOC_IDS=

# Comma-separated "<resource_id>:<lang>" pairs (e.g. "7:en,9:fr") used by
# k6-load-tests/scenarios/document-download.js to fetch /resources/download/<id>/<lang>.
K6_RESOURCE_IDS=

# Preissued AI Bearer JWT (obtain via GET /api/ai/v2/token while logged in).
# Required by k6-load-tests/scenarios/ai-chat-smoke.js when K6_AI_CHAT_ENABLED=true.
K6_AI_TOKEN=

# Set to "true" to actually run the AI chat scenario. Default is no-op (cost guard).
K6_AI_CHAT_ENABLED=false

# Set to "true" to actually run the write-smoke scenario. THROWAWAY STAGING DB ONLY.
K6_WRITE_ENABLED=false

# Endpoint used by write-smoke.js when enabled. Default chosen for low impact;
# coordinate with ops before changing.
K6_WRITE_PATH=/api/v1/indicator-suggestions
